# Production environment — baked into the frontend bundle at build time.
# The server runs at the same origin on /api and /socket.io,
# proxied by nginx, so we use a relative path prefix.
VITE_API_URL=https://bughouse.ai